package ca.ulaval.glo4003.trotti.infrastructure.config.binders;

import org.glassfish.hk2.utilities.binding.AbstractBinder;

public class ApplicationServiceBinder extends AbstractBinder {

    @Override
    protected void configure() {
//        Binding examples:
//        1 - bindAsContract(OrderApplicationService.class);
//        2 - bind(OrderController.class).to(OrderResource.class);
//        3 - bind(new OrderPersistenceInMemory(orderDatabase)).to(OrderRepository.class);
    }
}
